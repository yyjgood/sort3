

共两个文件chaxun.cpp和ll.cpp。

chaxun.cpp 用来获取文件中的行数M,以方便后续建立二维数组

ll.cpp 用来实现排序，并将排序结果输入到文件中

默认输入文件为“p“，输出文件为”p.txt“，可在源代码中更改输入输出文件。

大致运行过程介绍：

1 首先从p文件中读入数据，输入到一个二维数组a[M][N]。N的值为4，M的值由具体的文件决定，可以利用chaxun.cpp获取。

2 获得M的值后，在ll.cpp中更新，运行ll.cpp即可实现转换。

ps： 不需要建立工程，直接用 codeblocks 打开cpp文件，按F9即可运行得到结果。
